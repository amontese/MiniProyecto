import 'package:flutter/material.dart';

class PaginaReporte extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Reporte"),
    );
  }
}
